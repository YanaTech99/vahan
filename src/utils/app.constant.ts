export class AppConstant {
  public static readonly API_CONSTANTS = {
    TIMEOUT_MS: 10000,
    TIMEOUT_MESSAGE: 'API TIME OUT',
  };

  public static readonly NAME_ADDED_TYPE = {
    MANUAL: 'manual',
    AUTO: 'auto',
  };

  public static readonly DATE_FORMAT = {
    DD_MM_YYYY: 'DD-MMM-YYYY',
    DD_MMMM_YYYY: 'DD-MMMM-YYYY',
  };

  public static readonly TIME_FORMAT = {
    MM_SS: 'MM:SS',
  };

  public static EVENT_DATE_TIME_FORMAT = 'dddd, MMM DD @ h A';
}
